// Build constants for the phone-side (PebbleKit JS) code.
//
// GITHUB_CLIENT_ID is the *public* client ID of the GitHub App used for the
// OAuth device-flow sign-in. It is safe to commit — device flow requires no
// client secret. Users may override it from the phone settings (advanced field).
module.exports = {
  GITHUB_CLIENT_ID: "Iv23liM7MD2lO33S21Dy",
};
